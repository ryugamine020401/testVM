### Name

        README.md

### Step

        - install
        npm install socket.io
        npm install peer

        - run code
        node server.js

        - go to
        localhost:3001/index.html

### Auto Refresh

        - install
        npm install nodemon

        - add to package.json
        "scripts": {
                "test": "nodemon server.js"
        }

        - run code
        npm run test